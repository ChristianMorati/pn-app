import colors from "tailwindcss/colors";


const colorName = 'neutral';

export const themeColors = {
    primary: colors[colorName][500],
    secondary: colors[colorName][700],
    basePage: colors[colorName][900],
    color: colors.white,
}